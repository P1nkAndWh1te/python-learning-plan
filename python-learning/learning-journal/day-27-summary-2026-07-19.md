# Day 27 学习总结：真实 Embedding 方案研究

**日期**：2026年7月19日  
**阶段**：第三阶段 — RAG 项目实战（Day 27/30）

---

## 今日完成情况

状态：完成真实 embedding 替换方案研究。

今天没有修改主功能代码，而是明确了 Dify、教学版 embedding 和真实 embedding 的定位。

新增文件：

```text
05_复盘与沉淀/day27_embedding_solution_research.md
```

---

## 今日结论

Dify：

```text
作为低代码 RAG 体验和对照组，不作为当前 Streamlit 主项目依赖。
```

教学版 embedding：

```text
保留为 baseline，不作为最终方案。
```

真实 embedding 推荐路线：

```text
Day28 先用 Chroma 默认 embedding function 做最小验证。
如果中文效果不理想，再评估 BGE 中文模型。
```

---

## 当前依据

当前教学版 embedding 的 10 题评测结果是：

```text
命中率：70%
```

这个数字后续会作为 baseline，用来对比真实 embedding 是否带来提升。

---

## 今日理解

技术选型不能只看“哪个模型更强”，还要考虑：

- 当前系统是否容易接入。
- 本地 Python 环境是否兼容。
- 安装成本和下载成本是否可接受。
- 是否能和已有评测 baseline 做对比。

---

## 下一步

Day28 新增独立实验脚本，使用 Chroma 默认 embedding function 跑同一份 FAQ 和同一组 10 题评测。
