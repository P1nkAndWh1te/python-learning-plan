# Day 22：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 22/30）**

今天根据 Day21 的检索评估结果优化检索质量。Day21 发现所有问题都返回同一组 Top chunks，说明切分粒度和关键词维度区分能力不足。

---

## 今日目标

完成后，你应该能：

- 解释为什么 chunk 切分方式会影响检索质量。
- 理解按 Markdown 标题切分比纯字符切分更适合 FAQ 文档。
- 看懂关键词维度过泛会造成检索结果趋同。
- 观察优化前后 Top chunks 是否更有区分度。

---

## 今日任务

修改：

```text
04_成果输出/rag-qa-system/app.py
```

新增或调整：

- 将默认 chunk size 从 500 调整为 350。
- 将默认 overlap 从 100 调整为 50。
- 优先按 Markdown `##` 标题切分文档。
- 对没有标题的普通文本继续使用固定长度切分。
- 增强 API、embedding、Chroma、DeepSeek 等关键词维度。
- 减少过泛关键词造成的误匹配。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- Markdown 标题切分可以通过最小单元验证。
- 使用 `python_learning_faq.md` 评估时，不同问题能检索出更有区分度的 Top chunks。
- Streamlit 页面可以启动。
- Day22 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day23 将开始把检索评估结果沉淀成项目评测记录，为后续简历和项目展示准备证据。
