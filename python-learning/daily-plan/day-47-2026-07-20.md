# Day 47：RRF Baseline 实验

## 今日目标

继续 DocuAsk v2 升级，新增独立 RRF service，把向量检索排名和 BM25 排名融合。

## 背景

Day46 已经完成 BM25 baseline。

今天继续实现 RRF：

```text
vector ranking + bm25 ranking -> rrf ranking
```

## 今日任务

1. 新增 `backend/services/rrf.py`。
2. 实现 `reciprocal_rank_fusion`。
3. 实现 `retrieve_relevant_chunks_rrf`。
4. 新增独立检查脚本，输出 vector / BM25 / RRF 的 Top-3。
5. 新增 pytest 覆盖 RRF 对 RAG 流程问题的召回。

## 今日边界

- 不改 `/qa` 默认行为。
- 不改 `/evaluation` 默认行为。
- 不接 DeepSeek。
- 不做 RRF 参数调优。

## 今日验收标准

今天算完成，当且仅当：

- RRF service 可独立导入。
- `RAG 的基本流程是什么？` RRF Top-1 命中 Chunk 6。
- `python -m pytest -q` 通过。
- 原 Day33 指标不变。
- GitHub 上有 Day47 提交。
