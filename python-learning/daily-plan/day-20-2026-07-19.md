# Day 20：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 20/30）**

今天把 Day19 组织好的上下文交给 DeepSeek，让模型基于检索到的 chunk 生成最终回答。

---

## 今日目标

完成后，你应该能：

- 说清楚 RAG 中生成步骤的位置。
- 理解为什么 prompt 必须限制 LLM 只能根据检索资料回答。
- 在 Streamlit 页面中看到最终回答和来源引用。
- 区分 API 认证问题、检索问题和生成问题。

---

## 今日任务

修改：

```text
04_成果输出/rag-qa-system/app.py
```

新增能力：

- 从环境变量读取 `DEEPSEEK_API_KEY`。
- 使用 OpenAI-compatible SDK 连接 DeepSeek。
- 将检索到的 chunk 格式化为 prompt 上下文。
- 要求模型只根据资料回答。
- 要求回答保留来源引用。
- 对缺少 API Key、额度不足和请求失败做页面提示。

今天暂时不做：

- 真实语义 embedding。
- PDF 解析。
- 长文档持久化。
- 评测集批量测试。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- prompt 构造函数可以通过最小单元验证。
- Streamlit 页面可以启动。
- 如果当前环境没有 `DEEPSEEK_API_KEY`，页面能给出明确提示。
- 设置 `DEEPSEEK_API_KEY` 后，上传文档并提问可以得到 DeepSeek 生成的回答。
- Day20 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day21 开始做检索质量优化，重点是调整 chunk、关键词维度和测试问题，观察 RAG 回答质量如何变化。
