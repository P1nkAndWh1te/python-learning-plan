# Day 31：评测集复盘与检索指标修正

## 今日目标

复盘 Day30 的失败题，判断问题来自模型、文档切分，还是评测标准。

## 背景

Day30 得到的结果是：

```text
教学版关键词 embedding：70%
Chroma 默认 embedding：40%
BGE 中文 embedding：70%
```

但复盘后发现，第 3 题：

```text
OpenAI API 额度和 ChatGPT Plus 互通吗？
```

答案实际在 Day8 段落，也就是 `Chunk 2`，不是原先标记的 `Chunk 3`。

因此 Day31 要修正评测口径，并同时观察：

- strict top-1 hit：第一名是否就是 expected chunk。
- top-k recall：expected chunk 是否出现在前三个检索结果中。

## 今日任务

1. 修正第 3 题 expected chunk。
2. 在主应用评测表中增加 `top_k_hit`。
3. 新增独立评测复盘脚本。
4. 对比教学版关键词 embedding、Chroma 默认 embedding、BGE 中文 embedding。
5. 记录新的评测结论。

## 今日边界

- 不直接把主应用切换到 BGE。
- 不调整 FAQ 原文。
- 不为了提高分数随意修改问题。
- 不调用 DeepSeek 生成答案。

## 今日验收标准

今天算完成，当且仅当：

- 能输出三种 embedding 的 top-1 和 top-k 对比。
- 能解释为什么 top-1 和 top-k 要分开看。
- 有 Day31 复盘记录。
- GitHub 上有 Day31 提交。

