# Day 46：BM25 Baseline 实验

## 今日目标

继续 DocuAsk v2 升级，新增独立 BM25 关键词检索 baseline，为后续 RRF 混合检索做准备。

## 背景

当前项目已有：

- Teaching keyword embedding。
- BGE Chinese embedding。
- Chroma 向量检索。
- 10 题 evaluation endpoint。
- pytest 自动化测试。

下一阶段要做 Hybrid Search，但不能直接上 RRF。今天先把 BM25 单独跑通。

## 今日任务

1. 新增 `backend/services/bm25.py`。
2. 实现基础 tokenizer。
3. 实现 BM25 scoring。
4. 实现 `retrieve_relevant_chunks_bm25`。
5. 新增独立检查脚本。
6. 新增 pytest 覆盖 BM25 对 RAG 流程问题的召回。

## 今日边界

- 不引入 `jieba`。
- 不新增生产依赖。
- 不改 `/qa`。
- 不改 `/evaluation`。
- 不做 RRF 融合。

## 今日验收标准

今天算完成，当且仅当：

- BM25 service 可独立导入。
- `RAG 的基本流程是什么？` Top-1 命中 Chunk 6。
- `python -m pytest -q` 通过。
- 原 Day33 指标不变。
- GitHub 上有 Day46 提交。
