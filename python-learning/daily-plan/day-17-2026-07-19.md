# Day 17：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 17/30）**

今天继续完善正式 RAG 项目，只做一个核心目标：把读取到的一整篇文档拆成多个 chunk，并在页面中展示出来。

---

## 今日目标

完成后，你应该能：

- 解释为什么 RAG 不能总是把整篇文档一次性交给模型。
- 理解 chunk size 和 chunk overlap 的作用。
- 在 Streamlit 页面中查看每个 chunk 的编号、长度和内容。
- 说清楚文档切分是 embedding 和向量检索的前置步骤。

---

## 今日任务

修改：

```text
04_成果输出/rag-qa-system/app.py
```

新增能力：

- 将上传后的文本按固定字符数切分。
- 默认每个 chunk 500 个字符。
- 相邻 chunk 保留 100 个字符重叠。
- 页面显示 chunk 总数。
- 页面展开查看每个 chunk 的内容。

今天暂时不做：

- 按语义切分。
- embedding。
- Chroma 入库。
- LLM 回答。
- PDF 解析。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- 文档切分函数可以通过最小单元验证。
- Streamlit 页面可以启动。
- 上传 TXT 或 Markdown 后，页面能显示 chunk 总数和每个 chunk。
- Day17 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day18 将开始把 chunk 转成 embedding，并尝试接入 Chroma 做本地向量存储。
