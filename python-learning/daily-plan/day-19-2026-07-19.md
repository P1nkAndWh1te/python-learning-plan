# Day 19：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 19/30）**

今天继续完善正式 RAG 项目，不引入新的模型能力，只优化检索结果展示，让页面更接近真实 RAG 系统的输出结构。

---

## 今日目标

完成后，你应该能：

- 区分检索结果、上下文和最终回答。
- 理解 source references 为什么重要。
- 看懂 Chroma 返回的 distance 含义。
- 说清楚哪些 chunk 会被交给后续 LLM。

---

## 今日任务

修改：

```text
04_成果输出/rag-qa-system/app.py
```

新增能力：

- 显示用户问题命中的概念维度。
- 解释 Chroma distance：数值越小越相似。
- 将 Top chunks 格式化成后续 LLM 可用的上下文。
- 显示来源引用列表，例如 `Chunk 3`。
- 保留详细检索结果，方便展开检查。

今天暂时不做：

- 调用 DeepSeek。
- 真实语义 embedding。
- 自动生成最终自然语言答案。
- PDF 解析。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- 上下文格式化函数可以通过最小单元验证。
- Streamlit 页面可以启动。
- 上传 TXT 或 Markdown 后，输入相关问题能看到上下文和来源引用。
- Day19 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day20 将接入 DeepSeek，让模型基于检索到的上下文生成回答，并保留来源引用。
