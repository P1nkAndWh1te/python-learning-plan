# Day 53：多文档小样本评测

## 今日目标

把 `/evaluation` 从固定 FAQ 评测扩展为支持自定义 evaluation cases，为多文档小样本评测打基础。

## 今日任务

1. 给 `POST /evaluation` 增加可选 `evaluation_cases`。
2. 保持默认 10 题 FAQ 评测不变。
3. 新增 DocuAsk backend FAQ 样本文档。
4. 新增自定义评测用例。
5. pytest 覆盖自定义 evaluation cases。
6. 新增 Day53 多文档评测脚本。

## 今日边界

- 不做大规模评测。
- 不引入外部数据集。
- 不改默认 10 题评测结果。
- 不新增 rerank。

## 今日验收标准

- 不传 `evaluation_cases` 时，默认 10 题评测仍可用。
- 传入自定义 `evaluation_cases` 时，接口按自定义问题评测。
- 新增多文档评测脚本通过。
- `python -m pytest -q` 通过。
