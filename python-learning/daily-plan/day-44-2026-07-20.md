# Day 44：新增 Pytest 自动化测试

## 今日目标

继续 DocuAsk v2 升级，把前面手动验证的接口和检索指标沉淀成 `pytest` 自动化测试。

## 背景

Day42 和 Day43 已经完成：

- `POST /documents`
- `POST /qa`

之前主要靠脚本和手动输出验证。今天要把关键验收点转成测试用例。

## 今日任务

1. 安装并固定 `pytest` 版本。
2. 新增 `pytest.ini`。
3. 新增后端 API 测试。
4. 测试 `POST /documents` 能写入 FAQ 文档。
5. 测试 `POST /qa` 能召回正确 chunk。
6. 测试缺失 collection 返回 404。
7. 测试教学版 10 题召回指标保持稳定。

## 今日边界

- 不测试 BGE 指标，避免自动测试依赖模型下载和网络状态。
- 不测试 DeepSeek 生成，避免自动测试依赖外部 API Key。
- 不新增 FastAPI endpoint。
- 不改 Streamlit 页面。

## 今日验收标准

今天算完成，当且仅当：

- `python -m pytest -q` 通过。
- 至少覆盖 documents endpoint、qa endpoint、retrieval metrics。
- 测试不依赖 DeepSeek API Key。
- GitHub 上有 Day44 提交。
