# Day 40：抽离 Retrieval Service

## 今日目标

继续 DocuAsk v2 模块化升级，把 Chroma 建库和 Top-k 检索逻辑从 Streamlit `app.py` 中抽离到后端 service。

## 背景

Day38 已经抽离了文档切分逻辑。

Day39 已经抽离了 embedding 逻辑。

今天继续抽离：

- Chroma EphemeralClient。
- collection 创建。
- chunk embedding 写入。
- question embedding 查询。
- retrieved chunks 格式组装。

## 今日任务

1. 新增 `backend/services/retrieval.py`。
2. 将 `build_chunk_collection` 移入 retrieval service。
3. 将 `retrieve_relevant_chunks` 移入 retrieval service。
4. 将 `format_retrieved_context` 移入 retrieval service。
5. Streamlit `app.py` 改为导入复用 retrieval service。
6. 新增独立验证脚本。
7. 验证现有 10 题指标不变。

## 今日边界

- 不改成 Chroma PersistentClient。
- 不新增 FastAPI endpoint。
- 不迁移 DeepSeek 生成。
- 不改 UI。
- 不改评测问题。

## 今日验收标准

今天算完成，当且仅当：

- `backend/services/retrieval.py` 可独立导入。
- `app.py` 不再直接 import `chromadb`。
- RAG 测试问题仍能召回 Chunk 6。
- Day33 指标仍为 Teaching 60/100、BGE 80/100。
- GitHub 上有 Day40 提交。
