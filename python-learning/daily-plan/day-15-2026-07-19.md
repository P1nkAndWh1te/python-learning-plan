# Day 15：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 15/30）**

今天开始把前面零散练习整合成一个正式项目。第一步不追求完整 RAG，而是先创建项目骨架，并跑通 Streamlit 最小页面。

---

## 今日目标

完成后，你应该能：

- 说清楚正式 RAG 项目的基础目录结构。
- 使用 Streamlit 创建一个可打开的本地网页应用。
- 在页面中提供文档上传、问题输入和提交按钮。
- 理解“先跑通界面入口，再接入后端链路”的项目开发顺序。

---

## 今日任务

创建正式项目：

```text
04_成果输出/rag-qa-system/
```

最小页面包含：

- 页面标题。
- 文档上传入口。
- 问题输入框。
- 提交按钮。
- 占位回答区域。

今天暂时不接入 LlamaIndex、Chroma 或 DeepSeek。它们会在后续几天逐步接入。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- Streamlit 页面可以在本地启动。
- 页面能看到上传入口、问题输入框和提交按钮。
- 用户点击提交后，能看到明确的占位回答。
- Day15 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day16 将开始接入文档读取：先支持 TXT 或 Markdown 文本，把上传文件内容读出来并显示预览。
