## Day 4：2026年7月13日（周一）

**当前阶段：第一阶段 — Python 速成（Day 4/7）**

---

### 今日任务

1. **文件读写**：`open()`、`read()`、`readline()`、`readlines()`、`write()`
2. **文件模式**：`r`（读）、`w`（写/覆盖）、`a`（追加）、`rb`（二进制读）
3. **`with` 语句**：自动关闭文件，不用手动 `close()`
4. **异常处理**：`try / except / finally`，捕获 `FileNotFoundError`、`IOError` 等

---

### 今日检验标准

学完后，你应该能：
- 读取一个文本文件，统计其中有多少行、多少个单词
- 把一个列表的内容逐行写入一个新文件
- 用 `try/except` 优雅地处理文件不存在的情况
- 解释 `with open()` 比 `open() + close()` 好在哪

---

### 推荐学习资源

| 资源 | 链接 |
|------|------|
| 廖雪峰 Python 教程 — 文件读写 | https://www.liaoxuefeng.com/wiki/1016959663602400/1017607179232640 |
| 廖雪峰 Python 教程 — 错误处理 | https://www.liaoxuefeng.com/wiki/1016959663602400/1017598873256736 |

---

### 小练习

```python
# 1. 创建一个 test.txt，写入三行文字，然后读出来打印
# 2. 读取一个不存在的文件，用 try/except 捕获错误并打印"文件不存在"
# 3. 读取一个 CSV 文件（自己随便造几行），用 split(',') 解析每一行
```

---

今天是从"写脚本"到"处理数据"的转折点，文件读写是后面项目里加载文档、保存结果的基础。打开编辑器，动手敲起来！