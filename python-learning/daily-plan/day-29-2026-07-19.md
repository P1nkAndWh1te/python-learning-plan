# Day 29：中文 Embedding 依赖评估

## 今日目标

评估是否适合把项目从教学版关键词 embedding 推进到中文真实 embedding。

今天不直接安装新依赖，先确认：

- 当前 Python 版本。
- 当前已安装的相关包。
- `sentence-transformers` 会带来的依赖范围。
- 是否值得进入中文 embedding 实验。

## 背景

Day28 已经验证：

```text
Chroma 默认 embedding function 可以跑通，但中文 FAQ 命中率只有 40%。
```

这说明默认 embedding 不是当前项目的最终方案。下一步应该评估中文 embedding 模型，而不是直接把默认模型接入主应用。

## 今日任务

1. 检查当前 Python 环境。
2. 检查是否已安装 `torch`、`sentence-transformers`、`transformers`。
3. 使用 `pip install --dry-run sentence-transformers` 观察依赖解析。
4. 记录安装成本和风险。
5. 决定是否进入下一步中文 embedding 实验。

## 今日边界

- 不安装新生产依赖。
- 不修改 Streamlit 主应用。
- 不改评测问题。
- 不更换 DeepSeek 生成模型。

## 今日验收标准

今天算完成，当且仅当：

- 有依赖检查脚本。
- 有依赖评估记录。
- 能说清楚为什么新增 embedding 依赖需要确认。
- GitHub 上有 Day29 提交。

