# Day 39：抽离 Embedding Service

## 今日目标

继续 DocuAsk v2 模块化升级，把 embedding 相关逻辑从 Streamlit `app.py` 中抽离到后端 service。

## 背景

Day38 已经抽离了文档切分逻辑。

今天继续抽离：

- Teaching keyword embedding。
- BGE Chinese embedding。
- embedding mode 常量。
- Chroma collection name 映射。
- matched concepts 计算。

## 今日任务

1. 新增 `backend/services/embeddings.py`。
2. 将 `CONCEPTS`、embedding mode 常量和 collection name 映射移入 service。
3. 将 `embed_text`、`embed_text_with_bge`、`embed_for_mode`、`get_matched_concepts` 移入 service。
4. Streamlit `app.py` 改为导入复用 embedding service。
5. 新增独立验证脚本。
6. 验证关键词 embedding 是 12 维，BGE embedding 是 512 维。
7. 验证现有 10 题指标不变。

## 今日边界

- 不迁移 Chroma 检索。
- 不迁移 DeepSeek 生成。
- 不新增 FastAPI endpoint。
- 不改 UI。
- 不改评测问题。

## 今日验收标准

今天算完成，当且仅当：

- `backend/services/embeddings.py` 可独立导入。
- 关键词 embedding 输出 12 维。
- BGE embedding 输出 512 维。
- Day33 指标仍为 Teaching 60/100、BGE 80/100。
- GitHub 上有 Day39 提交。

