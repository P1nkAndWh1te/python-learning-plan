## Day 5：2026年7月14日（周二）

**当前阶段：第一阶段 — Python 速成（Day 5/7）**

---

### 今日任务

1. **pip 包管理**：`pip install`、`pip uninstall`、`pip list`、`pip freeze > requirements.txt`
2. **虚拟环境 venv**：创建、激活、退出，理解为什么要隔离环境
3. **requests 库**：发送 GET/POST 请求，获取网页数据，理解 `status_code`、`json()`、`text`

---

### 今日检验标准

学完后，你应该能：
- 创建一个虚拟环境，在里面安装 requests 库，导出 `requirements.txt`
- 用 requests 获取一个 API 的数据（比如天气 API 或 GitHub API），解析 JSON 并打印结果
- 解释为什么每个项目需要独立的虚拟环境

---

### 小练习

```python
# 1. 用 requests 访问 https://api.github.com，打印状态码和返回的 JSON 中的关键字段
# 2. 自己写一个 get_weather(city) 函数，用免费天气 API 获取城市天气（找不到免费 API 就用模拟数据代替）
# 3. 把当前项目的依赖导出到 requirements.txt
```

---

### 推荐学习资源

| 资源 | 链接 |
|------|------|
| 廖雪峰 Python 教程 — 虚拟环境 | https://www.liaoxuefeng.com/wiki/1016959663602400/1019273143120480 |
| requests 官方文档（中文） | https://docs.python-requests.org/zh_CN/latest/ |
| GitHub API 文档 | https://docs.github.com/zh/rest |

---

今天是从"本地脚本"迈向"联网项目"的一天，requests 是后面调用 OpenAI API 时你最常用的工具。打开编辑器，动手敲起来！