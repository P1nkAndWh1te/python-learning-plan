# Day 30：BGE 中文 Embedding 评测

## 今日目标

在独立脚本中使用中文 embedding 模型评测当前 RAG FAQ 的检索效果。

使用模型：

```text
BAAI/bge-small-zh-v1.5
```

## 背景

已有对比结果：

```text
教学版关键词 embedding：70%
Chroma 默认 embedding：40%
```

Day30 的目标不是直接改主应用，而是先验证中文 embedding 路线是否值得继续。

## 今日任务

1. 安装 `sentence-transformers`。
2. 验证 `torch`、`sentence-transformers`、`transformers` 能正常导入。
3. 加载 `BAAI/bge-small-zh-v1.5`。
4. 新增独立 BGE 评测脚本。
5. 跑同一份 FAQ 和同一组 10 题评测。
6. 记录结果和是否迁移主应用的判断。

## 今日边界

- 不修改 Streamlit 主应用。
- 不改 10 题评测集。
- 不使用 DeepSeek 生成答案。
- 不把 embedding 模型直接接入生产路径。

## 今日验收标准

今天算完成，当且仅当：

- BGE 模型能成功加载。
- 独立脚本能完成 10 题评测。
- 有命中率对比记录。
- 有 Day30 学习总结。
- GitHub 上有 Day30 提交。

