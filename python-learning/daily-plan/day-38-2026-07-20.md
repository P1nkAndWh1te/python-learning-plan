# Day 38：抽离文档切分服务

## 今日目标

继续 DocuAsk v2 Step 1，把文档切分逻辑从 Streamlit `app.py` 中抽离为可复用 service。

## 背景

Day37 已经完成 FastAPI 后端服务骨架和 `/health`。

下一步要让后端和 Streamlit 后续能复用同一套 RAG 核心逻辑，先从风险最低的 chunking 开始。

## 今日任务

1. 新增 `backend/services/`。
2. 新增 `chunking.py`。
3. 将 `split_text_into_chunks`、`split_markdown_sections`、`split_by_character_count` 抽离到 service。
4. Streamlit `app.py` 改为导入复用 chunking service。
5. 新增独立验证脚本。
6. 验证 FAQ 仍然切成 6 个 chunks。

## 今日边界

- 不迁移 embedding。
- 不迁移 Chroma 检索。
- 不迁移 DeepSeek 生成。
- 不新增 API endpoint。
- 不改 UI。

## 今日验收标准

今天算完成，当且仅当：

- `backend/services/chunking.py` 可独立导入。
- Streamlit `app.py` 语法检查通过。
- FAQ 仍然切出 6 个 chunks。
- 现有 Day33 指标检查脚本仍然通过。
- GitHub 上有 Day38 提交。

