# Day 16：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 16/30）**

今天继续完善正式 RAG 项目，但仍然只做一个小目标：让上传的 TXT 或 Markdown 文件真正进入程序，并在页面中显示内容预览。

---

## 今日目标

完成后，你应该能：

- 解释上传文件后，程序如何拿到文件内容。
- 区分“文件已上传”和“文件内容已读取”。
- 在 Streamlit 页面中显示文档基本信息。
- 在页面中预览上传文档内容。

---

## 今日任务

修改：

```text
04_成果输出/rag-qa-system/app.py
```

新增能力：

- 只允许上传 `.txt` 和 `.md`。
- 读取上传文件的字节内容。
- 尝试用 UTF-8 或 GBK 解码文本。
- 显示文件名、大小、字符数和编码。
- 展示文档前 2000 个字符作为预览。

今天暂时不做：

- PDF 解析。
- 文档切分。
- embedding。
- Chroma 入库。
- LLM 回答。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- 文本读取函数可以通过一个最小单元验证。
- Streamlit 页面可以启动。
- 上传 TXT 或 Markdown 后，页面能显示文档预览。
- Day16 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day17 将开始做文档切分，把一整篇文档拆成适合检索的小片段。
