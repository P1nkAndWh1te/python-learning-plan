# Day 25：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 25/30）**

今天继续完善检索评测，不新增大功能。目标是把固定测试问题从 5 个扩展到 10 个，并为每个问题标注预期命中的 chunk。

---

## 今日目标

完成后，你应该能：

- 理解 `expected_top_chunk` 的作用。
- 用固定问题判断检索是否命中预期。
- 区分“能返回结果”和“返回了正确结果”。
- 为后续项目评测报告准备更完整的数据结构。

---

## 今日任务

修改：

```text
04_成果输出/rag-qa-system/app.py
```

新增或调整：

- 将固定评测问题扩展到 10 个。
- 每个问题增加 `expected_top_chunk`。
- 评测表增加 `hit` 字段。
- 页面显示整体命中率。
- 继续显示 matched concepts、top chunks 和 best distance。
- 根据新增问题补充少量关键词。

今天暂时不做：

- 真实语义 embedding。
- PDF 解析。
- 批量调用 DeepSeek。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- 评测函数可以返回 10 条结果。
- 每条结果包含 expected top chunk 和 hit 字段。
- 使用 `python_learning_faq.md` 测试时，页面能显示 10 个问题和整体命中率。
- Streamlit 页面可以启动。
- Day25 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day26 将把 10 题评测结果沉淀为正式评测表，并准备准确率统计。
