# Day 18：2026年7月19日（周日）

**当前阶段：第三阶段 — RAG 项目实战（Day 18/30）**

今天继续完善正式 RAG 项目，目标是把 Day17 生成的 chunks 转成向量，并放入 Chroma 做本地检索。

---

## 今日目标

完成后，你应该能：

- 说清楚 chunk 为什么要转成 embedding。
- 理解向量数据库存的是“文本片段 + 向量 + 元数据”。
- 在 Streamlit 页面中输入问题，并看到 Chroma 检索出的相关 chunk。
- 区分“检索结果”和“LLM 最终回答”。

---

## 今日任务

修改：

```text
04_成果输出/rag-qa-system/app.py
```

新增能力：

- 使用关键词计数生成教学版 embedding。
- 将每个 chunk 写入 Chroma 临时 collection。
- 将用户问题转成同一套 embedding。
- 从 Chroma 检索 Top 3 相关 chunk。
- 页面显示检索结果、chunk 编号和距离。

今天暂时不做：

- 真实语义 embedding。
- 持久化 Chroma 数据库。
- LlamaIndex 封装。
- DeepSeek 生成最终回答。
- PDF 解析。

---

## 今日检验标准

今天算完成，当且仅当：

- `app.py` 可以通过 Python 语法检查。
- embedding 函数和 Chroma 检索可以通过最小单元验证。
- Streamlit 页面可以启动。
- 上传 TXT 或 Markdown 后，输入相关问题能检索到 chunk。
- Day18 计划、代码和总结提交到 GitHub。

---

## 下一步预告

Day19 将优化检索结果展示，开始加入来源引用和更接近真实 RAG 的回答结构。
