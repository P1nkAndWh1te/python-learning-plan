# Day 50：API 文档与架构说明

## 今日目标

整理 DocuAsk 当前已经实现的后端 API、模块边界、数据流和评测结果，为后续项目介绍和面试讲解做材料沉淀。

## 今日背景

Day37 到 Day49 已经完成：

```text
FastAPI backend
RAG services
Chroma PersistentClient
pytest
BM25
RRF
/qa retrieval_mode
/evaluation retrieval_mode
```

今天不继续堆功能，先把当前能力讲清楚。

## 今日任务

1. 新增 `API.md`。
2. 新增 `ARCHITECTURE.md`。
3. 更新项目 README 中过期的限制说明。
4. 明确当前已实现能力和不能夸大的边界。
5. 记录 Day50 复盘和学习总结。

## 今日边界

- 不改后端业务代码。
- 不接 DeepSeek 到 FastAPI。
- 不新增依赖。
- 不声称生产级、多用户、PDF、rerank 或大规模评测。

## 今日验收标准

- `API.md` 能说明 `/health`、`/documents`、`/qa`、`/evaluation`。
- `ARCHITECTURE.md` 能说明模块结构、数据流、service 边界和当前限制。
- README 不再写“Chroma 没有持久化知识库”这类过期描述。
- `python -m pytest -q` 通过。
- GitHub 上有 Day50 提交。
