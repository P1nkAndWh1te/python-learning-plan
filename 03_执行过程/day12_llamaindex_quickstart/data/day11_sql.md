# Day11：SQL

Day11 学习了 SQL 基础，包括 `select`、`where`、`order by`、`group by` 和 `join`。

SQL 适合查询结构化表格数据，例如学生表、课程表、订单表和用户表。

向量检索适合查询非结构化文档语义，例如 Markdown、PDF、Word 文档里的自然语言内容。

`group by` 用于分组统计，`join` 用于连接多张表。

